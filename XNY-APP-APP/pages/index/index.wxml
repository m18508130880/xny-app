<view class="page">
  <view class="userinfo">
    <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
    <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    <image bindtap="Login" class="login" src="../../image/login.gif"></image>
    <!-- 有属性
    <text>avatarUrl:{{userInfo.avatarUrl}}</text>
    <text>nickName:{{userInfo.nickName}}</text>
    <text>country:{{userInfo.country}}</text>
    <text>gender:{{userInfo.gender}}</text>

        无属性
    <text>province:{{userInfo.province}}</text>
    <text>city:{{userInfo.city}}</text>
    <text>watermark:{{userInfo.watermark}}</text>
    <text>openId:{{userInfo.openId}}</text>
    <text>unionId:{{userInfo.unionId}}</text>
    <text>timestamp:{{userInfo.timestamp}}</text>
    <text>appid:{{userInfo.appid}}</text>
    -->
  </view>
</view>